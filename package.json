{
  "name": "atlasclock",
  "version": "1.0.0",
  "private": true,
  "description": "Atlasclock — a calm, offline-first world time dashboard.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js",
    "build": "node scripts/build.js"
  }
}

